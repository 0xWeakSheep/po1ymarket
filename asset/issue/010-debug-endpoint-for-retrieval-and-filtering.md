# 内部调试接口：retrieval 与 filtering

## 问题

当前推荐接口只返回最终结果，调试 retrieval 和 filtering 仍不够直接。

## 目标

提供一个内部调试接口，返回：

- 原始候选
- 打分后候选
- 被过滤原因

## 最小范围

- 仅内部可用
- 不作为正式对外 API

## 验收标准

- 开发者可单次请求看清“候选是如何变空的”
- 不影响现有公开接口
